# Lean requirements for the Streamlit Community Cloud deployment (app/).
# Deliberately does NOT include eval/dev dependencies (pytest, nbformat,
# pyyaml, langchain-classic, langchain-text-splitters's transitive extras,
# etc.) -- see requirements.txt / requirements-dev.txt for phase 1/2's
# fuller sets. This file is what actually gets installed on the deployed
# instance; keep it minimal, every extra package is memory the 1GB
# ceiling doesn't have to spare.

# CPU-only torch build -- the default PyPI wheel bundles CUDA and is far
# larger on disk/at import than this app (no GPU on Streamlit Community
# Cloud) needs.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu

streamlit==1.59.2
langchain-core==1.4.9
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
sentence-transformers==5.6.0
pypdf==6.14.2

# Optional LLM-generation arm (off by default; only exercised if a user
# pastes an API key in the sidebar -- see app/app_logic.py generate_answer).
# Lightweight to import; installed unconditionally so the feature works
# without a redeploy, but never required to have a valid key.
langchain-openai==1.3.5
